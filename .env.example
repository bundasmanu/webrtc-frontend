# Build-time defaults (Vite). Copy to .env for local dev.

# WebSocket SIP endpoint (Kamailio edge)
VITE_SIP_WSS_URI=wss://203.0.113.10:443

# Optional: JSON array of RTCIceServer (default: Google STUN)
# VITE_ICE_SERVERS_JSON=[{"urls":"stun:stun.l.google.com:19302"}]

# VITE_JSSIP_DEBUG=true
